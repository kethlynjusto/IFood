// const funcoes = require('./funcoesAuxiliares');

//console.log(funcoes);

//console.log(funcoes.gets()); //pega exatamente o que o gets faz

//const { nome } = pessoa;   as duas formas são iguais, a diferença que esse posso colocar mais de um argumento
//const nome = pessoa.nome;  esse aqui vai individualmente.


const {gets, print} = require('./aula_funcoesAuxiliares');

print(gets());